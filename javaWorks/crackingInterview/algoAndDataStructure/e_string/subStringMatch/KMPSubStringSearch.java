public class KMPSubStringSearch {
    /**
     * Given a String 's' and pattern 'p' validate if p is a sustring of s
     */

    /**
     * Naive way is straight forward. We compare each characters with the characters
     * in 'p'
     * 
     * This is O(nm)
     */

    /**
     * Knuth Morris Pratt - can do it in O(n + m)
     * 
     * First, we need to pre-process the pattern and find its LPS (prefix which is a
     * sufix) ????
     * 
     * Then use the LPS array, validate the string
     */

    /**
     * 
     */
}