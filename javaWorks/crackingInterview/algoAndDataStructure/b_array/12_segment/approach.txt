If we need to collecte MAX, MIN, SUM, MAX_SUM, MIN_SUM in a given array which gets updated dynamically.

1. Use segement tree - SegmentTree.java - If array size is constant and but values change
2. Use SortedSet/SortedMap if the segment SIZE is not fixed and can change if possible.
