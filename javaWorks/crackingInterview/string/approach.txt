Preprocess the array or string if possible before any processing
like:
We can do the following 

Memorize:
    Using hasp map
    Using bit map
count
    and store in hasp map 
    or bit map
sort
reduce
reverse
split / append


Additional Questions: Object-Oriented Design (#7.12), Recursion (#8.3), Sorting and Searching (#10.9), (++
(#12.11), Moderate Problems (#16.8, #16.17, #16.22), Hard Problems (#17.4, #17.7, #17.13, #17.22, #17.26). 


For Questions like longest common sub sequence (m*n), longest palindrom substring (m*n), substring search (m*n RabinKarp)
We have an alternative, called Suffix tree which can do it in linear time (m+n).
Also, another alternative is Suffix array, which can also do it in linear time (m+n)
